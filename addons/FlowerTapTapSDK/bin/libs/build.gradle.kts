plugins {
    base //allows IDE clean to trigger clean on this module too
}

//Change group to whatever you want. Here I'm using the package from the aar that I'm importing from
group = "com.tapsdks.android"
version = "0.0.1"